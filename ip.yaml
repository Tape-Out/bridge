name: bridge
version: 0.1.0
spec: '0.1'
kind: library
lang: bsv
identity:
  slug: bridge-tapeout-bsv
  display_name: Bridges between buses
  summary: Bridges an APB4 host to AXI4-Lite and TL-UL targets through the 
    bus-neutral stalling contract.
  category: infrastructure
  maturity: simulated
deps:
  hwcore: ^0.1
  amba: ^0.1
  tilelink: ^0.1
area:
  probe:
    module: mkApb4ToAxi4LiteProbe
    src: htest/Probe.bsv
    check: true
  base:
    fixed: 966.32
  model: single-point
  error:
    bound: 0.05
    sign: over
    note: 'the isolated probe measures 866.32, a lower bound. A binder costs more in place than on its own: for APB4 the wraps of four IPs came out up to 96.04 above the neutral top while the probe gave 17.92, so amba is priced at 100. This bus has no row in the xirang bus table yet, so the in-place cost cannot be measured; the price is the probe plus 100 until an assembly measures it'
  corner:
    tool: ecc
    pdk: ics55
    freq_mhz: 100
    measured: '2026-09-16'
    toolchain: bsc 2026.01 · yosys 0.68+195 · ecc 0.1.0a11
    gen_digest: sha256:1169802d1c1d66fd
